version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://backend:3001/api
      - NEXT_PUBLIC_APP_NAME=Product Data Explorer
      - NEXT_PUBLIC_APP_DESCRIPTION=Explore products from World of Books with live scraping
      - NEXT_TELEMETRY_DISABLED=1
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

  # Backend service placeholder - uncomment and configure when backend is ready
  backend:
    # build:
    #   context: ../backend  # Adjust path to your backend directory
    #   dockerfile: Dockerfile
    image: nginx:alpine  # Temporary placeholder
    ports:
      - "3001:80"
    # environment:
    #   - NODE_ENV=production
    #   - DATABASE_URL=postgresql://user:password@db:5432/myapp
    networks:
      - app-network
    restart: unless-stopped
    # depends_on:
    #   - db

  # Database service - uncomment when needed
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     - POSTGRES_DB=myapp
  #     - POSTGRES_USER=user
  #     - POSTGRES_PASSWORD=password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - app-network
  #   restart: unless-stopped

  # Redis service - uncomment when needed for caching
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - app-network
  #   restart: unless-stopped

networks:
  app-network:
    driver: bridge

# volumes:
#   postgres_data: