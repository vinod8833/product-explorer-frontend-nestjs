[phases.setup]
nixPkgs = ['nodejs_20', 'npm']

[phases.install]
cmds = ['npm ci --only=production']

[phases.build]
cmds = [
  'npm ci',
  'npm run build'
]

[phases.start]
cmd = 'npm run start'

[variables]
NODE_ENV = 'production'